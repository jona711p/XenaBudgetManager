@{
    ViewBag.Title = "Regnskab";
}

<h2>Regnskab</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Regnskab</h4>
        <hr />
        <div class="form-group">
            @Html.Label("Vælg en Start Dato", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.Editor("fromDate", new { htmlAttributes = new { @class = "form-control", type = "date" } })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Vælg en Slut Dato", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.Editor("toDate", new { htmlAttributes = new { @class = "form-control", type = "date" } })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Se Regnskab" class="btn btn-default" />
            </div>
        </div>
    </div>
}
@*Claus - attempt to persist to db*@
<div id="gridContent">
    <input type="button" value="Opdater Kontoplan" onclick="location.href='@Url.Action("LedgerTag", "Accounting")'" />
</div>

@*@{
    var grid = new WebGrid(Model, canPage: true, rowsPerPage: 20,
    selectionFieldName: "selectedRow", ajaxUpdateContainerId: "gridContent");
    grid.Pager(WebGridPagerModes.NextPrevious);
}

<style type="text/css">
    .webGrid {
        margin: 4px;
        border-collapse: collapse;
        width: 500px;
        background-color: #B4CFC3;
    }

    .header {
        background-color: #C1D4E6;
        font-weight: bold;
        color: #FFF;
    }

    .webGrid th, .webGrid td {
        border: 1px solid #C0C0C0;
        padding: 5px;
    }

    .alt {
        background-color: #E4E9F5;
        color: #000;
    }

    .gridHead a:hover {
        text-decoration: underline;
    }

    .description {
        width: auto
    }

    .select {
        background-color: #71857C
    }
</style>

<div id="gridContent">
    @grid.GetHtml(tableStyle: "webGrid",
            headerStyle: "header",
            alternatingRowStyle: "alt",
            selectedRowStyle: "select",
            columns: grid.Columns(
            grid.Column("Month", "Måned"),
            grid.Column("Revenue", "Netto omsætning"),
            grid.Column("Revenue_DogFood", "Hunde mad"),
            grid.Column("Revenue_CatFood", "Katte mad"),
            grid.Column("ProductConsumption", "Vareforbrug"),
            grid.Column("ProductConsumption_DogFood", "Hunde mad"),
            grid.Column("ProductConsumption_CatFood", "Katte mad"),
            grid.Column("StaffCost", "Personale omkostninger"),
            grid.Column("StaffCost_Pay", "Løn"),
            grid.Column("LocalCost", "Lokale omkostninger"),
            grid.Column("LocalCost_Rent", "Leje af lokaler"),
            grid.Column("LocalCost_Light_Heat", "Lys/Varme"),
            grid.Column("MonthResult", "Måned Resultat")
     ))
</div>*@

